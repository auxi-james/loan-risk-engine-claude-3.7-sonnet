# Common properties
spring.application.name=Loan Risk

# Active profile
spring.profiles.active=dev

# JPA common settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
